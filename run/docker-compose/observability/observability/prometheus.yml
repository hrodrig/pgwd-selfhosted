# Prometheus scrape config for use with docker-compose.observability.yml (same directory as this file).
#
# Expects containers on the same Docker network (pgwd_edge) as this stack:
# - `pgwd` from run/docker-compose/traefik/docker-compose.yml
# - `traefik` (Prometheus metrics on :8080/metrics; enabled in that Traefik production Compose file)

global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: pgwd
    metrics_path: /api/pgwd/v1/metrics
    static_configs:
      - targets:
          - pgwd:8080
        labels:
          service: pgwd

  - job_name: node-exporter
    static_configs:
      - targets:
          - node-exporter:9100

  - job_name: traefik
    metrics_path: /metrics
    static_configs:
      - targets:
          - traefik:8080
