# Helper scripts

← [Back to run/README](../README.md).

| Script | Purpose |
|--------|---------|
| [`compose-stack.sh`](compose-stack.sh) | Run **`docker compose`** for **minimal** with **`--env-file`** and **`-f`** — paths resolve from the clone root. |
| **[`kubernetes-from-host/`](kubernetes-from-host/README.md)** | Example **bash** wrappers for **`pgwd`** using **`kubectl port-forward`** from a host (cron + heartbeat, **`PATH`**, sleep between runs, CLI flags vs **`/etc/pgwd`**). |

**Usage:** `./run/scripts/compose-stack.sh --help`

---

**[↑ Back to run/README](../README.md)**
