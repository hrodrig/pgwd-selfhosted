---
# full-cycle — clone/env → minimal stack up + log-based check → compose down
- name: Import compose setup
  ansible.builtin.import_playbook: setup.yml

- name: Import compose test
  ansible.builtin.import_playbook: test.yml

- name: Import compose teardown
  ansible.builtin.import_playbook: teardown.yml
